# Core voice-chat app (STT + LLM client + playback)
moonshine-voice>=0.1.1
openai>=1.40.0
python-dotenv>=1.0.1
sounddevice>=0.5.0
soundfile>=0.12.1
numpy>=1.26.0,<2.0.0
rich>=13.7.0
httpx>=0.27.0

# CosyVoice2 needs its own install — see scripts/setup_cosyvoice.sh
# and requirements-cosyvoice.txt
