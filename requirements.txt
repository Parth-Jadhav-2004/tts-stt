# CPU-only PyTorch wheels (keeps the install small; no CUDA).
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
piper-tts==1.6.0
vosk==0.3.45

# LLM (voice assistant). OpenAI-compatible API is used when OPENAI_API_KEY is
# set; otherwise a small local model runs offline via transformers + torch.
requests==2.34.2
torch==2.13.0+cpu
transformers==5.14.1
accelerate==1.14.0
